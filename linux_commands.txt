df -h 
   to show the amount of free disk space on each mounted disk.

free -hard
   this command outputs a summary of RAM usage.   

scp
   (Secure copy) we can copy files/directories from local to remote server

rsync
   ( this command is used to sync your local directory to server directory)

Hardlink:-

Softlink:- 

iwconfig
   ( Display's & change the parameters of the network interface which are specific to the wireless operation. e.g interface name, frequency, SSID).

